import { LeftSidebarPipe } from './left-sidebar.pipe';

describe('LeftSidebarPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftSidebarPipe();
    expect(pipe).toBeTruthy();
  });
});
