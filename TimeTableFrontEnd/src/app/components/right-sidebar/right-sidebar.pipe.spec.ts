import { RightSidebarPipe } from './right-sidebar.pipe';

describe('RightSidebarPipe', () => {
  it('create an instance', () => {
    const pipe = new RightSidebarPipe();
    expect(pipe).toBeTruthy();
  });
});
